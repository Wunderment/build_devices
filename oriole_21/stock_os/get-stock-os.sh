#!/bin/bash

# Exit as we're using the muppets.
exit

# Define where the security patch is stored for this device, rooted at "lineageos/device/google".
SECURITY_PATCH_FILE=gs101/device.mk

source ~/tasks/stock_os/get-pixel.sh